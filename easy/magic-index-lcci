// 这题有问题，如果单增的话可以用二分法，这题没说
class Solution {
public:
    int findMagicIndex(vector<int>& nums) {
        for(int i=0;i<nums.size();++i){
            if(nums[i]==i) return i;
        }
        return -1;
    }
};
